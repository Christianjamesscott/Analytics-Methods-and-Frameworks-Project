# Notebooks

The primary notebook is generated by the pipeline:

- `99_final_reproducible_notebook.ipynb`

It is intentionally lightweight and calls the same reusable code used by the terminal run path.

Refresh it with:

```powershell
python scripts/run_first_pass.py
```
